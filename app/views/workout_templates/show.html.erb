<h1><%= @workout_template.title %></h1>
<div class="timer">
    <h1 id="countdown">
      <span id="cd_s">60</span>
    </h1>
    <br/>
    <br/>
    <!-- <input type="button" value="Start" id="cd_start" />
    <input type="button" value="Pause" id="cd_pause" />
    <input type="button" value="Stop"  id="cd_stop" />
    <input type="button" value="Reset" id="cd_reset" />
    <br/>
    <br/>
    <span id="cd_status">Idle</span> -->
</div>
<div id="log-workout">
  <%= form_for :workouts, url: workouts_path, method: :post do |f| %>
    <%= f.hidden_field :workout_template_id, value: params[:id] %>
    <%= f.submit "Log Workout", class: "btn btn-success" %>
  <% end %>
</div>

<p><%= @workout_template.description %></p>

<br>
<%= link_to 'Back', category_path(@category) %>
